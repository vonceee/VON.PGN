- [x] Create the Lobby UI & invite partner flow
- [x] Implement simulated queue matching radar screen
- [x] Implement matchup VS countdown page
- [x] Integrate GMs bot list & search
- [x] Integrate matchmaking states into the gameplay component
- [x] Build and verify compilation compiles cleanly
- [x] Microservice Layer Changes
  - [x] Implement `getActiveGamesList()` and `broadcastActiveGames(io)` in `bughouse.handler.js`
  - [x] Make players join the `bughouse_global` room and receive the initial game list in `bughouse_join`
  - [x] Implement `bughouse_spectate` and `bughouse_leave_spectate` handlers
  - [x] Trigger broadcasts when matches start and when they end
- [x] Frontend Core Changes (`bughouse.component.ts`)
  - [x] Add `isSpectating` and `ongoingMatches` signals
  - [x] Handle `bughouse_active_games` socket event
  - [x] Update `bughouse_game_start` handler to support `isSpectator` mode
  - [x] Implement `spectateGame(gameId)` and update `exitToLobby()` for spectator cleanup
- [x] Frontend Template & Layout Changes
  - [x] Add spectator bar and lobby return button in `bughouse.component.html`
  - [x] Update inputs/outputs on the lobby tag in `bughouse.component.html`
- [x] Lobby Component Changes
  - [x] Add inputs/outputs to `bughouse-lobby.component.ts`
  - [x] Add "Live Matches" UI card to `bughouse-lobby.component.html`
